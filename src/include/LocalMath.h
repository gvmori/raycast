#ifndef _LOCALMATH_H_
    #define _LOCALMATH_H_

#include <cmath>
#include <algorithm>

namespace LocalMath {
    double RadToDeg(double degrees);
    double DegToRad(double degrees);
    constexpr double pi();
}

#endif
